#include <stdio.h>

// 需要再进行细化
/**
 * Note: The returned array must be malloced, assume caller calls free().
 */
double *twoSum(int n, int *returnSize)
{
  int total = 6 * n;
  for (int i = n; i <= total; i++)
  {
  }
  return returnSize;
}

int main()
{
  int a[] = {1, 2, 3, 4, 5};
  printf("%d\n", twoSum(1, (int)(sizeof(a) / sizeof(a[0])))); // true
  printf("%d\n", twoSum(2, (int)(sizeof(b) / sizeof(b[0])))); // true
  return 0;
}